require 'spec_helper'

describe Active::Domain::Rails do
  it 'has a version number' do
    expect(Active::Domain::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
